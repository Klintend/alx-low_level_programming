#include "main.h"
#include <stdio.h>
/**
 * main - the program starts here.
 * Return: always 0
 */
void _puts(char *s);

int main(void)
{
_puts("\"At the end of the day, my goal was to be the best hacker\"\n\t- Kevin Mitnick");
return (0);
}
